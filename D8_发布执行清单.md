# D8 发布执行清单（照着做）

> 目标：把 **噬菌体功能蛋白候选库 v2.0** 发布为 **GitHub 仓库 + Zenodo DOI + 可访问 Web 服务**，补齐 CNS 缺口 #7「可复现」。
>
> 发布包目录（本机）：`/Users/zhangwei/Documents/Codex/2026-08-30/ni-h/outputs/release_v2/phage-functional-proteins`（本地 git 仓库，已 commit，可直接 push）

## 0. 发布前核对（1 分钟）

```bash
cd /Users/zhangwei/Documents/Codex/2026-08-30/ni-h/outputs/release_v2/phage-functional-proteins
ls -la
sqlite3 data/candidates_v2.0.sqlite "select count(*) from candidates;"   # 期望 4117
git log --oneline
```

| 项 | 状态 |
|---|---|
| 数据/代码/图/Web 打包 | ✅ 已完成 |
| .zenodo.json / CITATION.cff / README / LICENSE | ✅ 已完成（DOI 占位，发布后回填） |
| GitHub 仓库 | ⬜ 第 1 步 |
| Zenodo DOI | ⬜ 第 2 步 |
| Web 部署 | ⬜ 第 3 步 |

已公开旧版：GitHub `LPS318/phage-functional-proteins`；Zenodo DOI `10.5281/zenodo.21971299`（v1.0）。v2.0 作为**新版本**发布。

## 1. GitHub 发布

### 1.1 准备凭据
用 HTTPS：首次 push 需要 **GitHub 用户名 + Personal Access Token**（不是密码）。没有就先去 GitHub → Settings → Developer settings → Personal access tokens 生成（勾 `repo`）。

### 1.2 关联远端并推送
```bash
cd /Users/zhangwei/Documents/Codex/2026-08-30/ni-h/outputs/release_v2/phage-functional-proteins
git remote add origin https://github.com/LPS318/phage-functional-proteins.git
git branch -M main
```

方案 A（推荐，最简单）—— 用 v2.0 覆盖远端 main：
```bash
git push -f origin main
```
> ⚠️ `-f` 会覆盖 GitHub 上的 v1.0 历史；但 **v1.0 已由 Zenodo 永久存档**（DOI 21971299），通常可接受。

方案 B（保留 v1.0 的 GitHub 历史）—— 推新分支再合并：
```bash
git fetch origin
git push -u origin main:v2.0
# 再到 GitHub 网页开 PR：v2.0 -> main
```

方案 C（最干净）—— 新建仓库 `phage-functional-proteins-v2`，`git remote set-url origin <新仓库>`，再 `git push -u origin main`。

### 1.3 打 tag 并建 Release
```bash
git tag -a v2.0.0 -m "Candidate Database v2.0"
git push origin main --tags
```
GitHub 仓库 → **Releases → Draft a new release** → Tag 选 `v2.0.0` → Title `Candidate Database v2.0` → 正文粘 README 的 "Key results (v2.0)" → **Publish release**。

## 2. Zenodo 发布（拿真实 DOI）

### 方式一（推荐）：GitHub 关联自动存档
1. 登录 <https://zenodo.org> → Settings → GitHub → 找到 `LPS318/phage-functional-proteins` → 打开开关。
2. 在 GitHub 建好并 Publish Release `v2.0.0`（见 1.3）。
3. Zenodo 自动抓取，几秒后在 Upload 页出现新草稿。
4. 打开草稿 → **Edit** 核对元数据（`.zenodo.json` 会带过来）：

| 字段 | 值 |
|---|---|
| Upload type | Dataset |
| Title | Phage Functional Protein Discovery — Candidate Database v2.0 |
| Creators | LPS318（可补全名/机构） |
| License | CC-BY-4.0 |
| Version | 2.0.0 |
| Related | isNewVersionOf → 10.5281/zenodo.21971299 |

5. 点 **Publish** → 生成新 **Version DOI**（形如 `10.5281/zenodo.XXXXXXX`）。记下 **Version DOI** 与 **Concept DOI**。

### 方式二（不关联 GitHub）：手动上传
```bash
cd /Users/zhangwei/Documents/Codex/2026-08-30/ni-h/outputs/release_v2
zip -r PhageFP_candidatesDB_v2.0.zip phage-functional-proteins -x '*/\.git/*'
```
Zenodo → **New upload** → 上传该 zip → 填同一套元数据（可从仓库 `.zenodo.json` 复制）→ Publish。
要与 v1.0 关联：Related works 里加 `isNewVersionOf: 10.5281/zenodo.21971299`。

## 3. Web 服务部署

### 3.1 本机先跑通（2 分钟）
```bash
cd /Users/zhangwei/Documents/Codex/2026-08-30/ni-h/outputs/release_v2/phage-functional-proteins/web
pip install -r requirements.txt
CAND_DB=../data/candidates_v2.0.sqlite uvicorn app:app --host 127.0.0.1 --port 8000
```
浏览器打开 `http://127.0.0.1:8000`；另开终端验证：
```bash
curl -s localhost:8000/api/stats        # 期望 {"total": 4117, ...}
```

### 3.2 Docker 打包（可选）
```bash
cd /Users/zhangwei/Documents/Codex/2026-08-30/ni-h/outputs/release_v2/phage-functional-proteins
docker build -t phage-cand-db ./web
docker run -d --name phage-cand-db -p 8000:8000 -v "$PWD/data":/data:ro phage-cand-db
curl -s localhost:8000/api/stats
```

### 3.3 放到公网（三选一）

**a) 阿里云 ECS（你已有账号，CPU 小规格即可，如 2C4G）**
```bash
# 在 ECS 上：装 docker → 传发布包 → 构建 → 常驻运行
docker build -t phage-cand-db ./web
docker run -d --restart=always --name phage-cand-db -p 80:8000 -v /opt/phage/data:/data:ro phage-cand-db
```
然后在安全组放行 80 端口；（可选）域名 A 记录指向 ECS 公网 IP。

**b) Render / Fly.io / Railway（免运维，最快上线）**
- 新建 Web Service，指向你的 GitHub 仓库；
- Build：`pip install -r web/requirements.txt`
- Start：`uvicorn web.app:app --host 0.0.0.0 --port $PORT`
- 环境变量：`CAND_DB=/app/data/candidates_v2.0.sqlite`

**c) 仅本地/局域网**：把 3.1 的 host 改成 `0.0.0.0` 即可。

### 3.4 验收
```bash
curl -s https://<你的域名>/api/stats
curl -s "https://<你的域名>/api/search?q=Klebsiella&limit=3"
curl -s "https://<你的域名>/api/export.csv?category=depolymerase" | head -2
```

## 4. 发布后回填（必做，否则引用不一致）

1. **发布包内**：把 `CITATION.cff` 的 `doi:` 与 `README.md` 顶部徽章换成真实 Version DOI，然后：
```bash
git add -A
git commit -m "docs: add real Zenodo DOI v2.0"
git push
```
2. **论文底稿**：把数据可用性声明里的 DOI 统一为真实的 v2.0 DOI。
   （注意：现稿写的是 `10.5281/zenodo.21971298`，旧 README 是 `...21971299` —— **两处必须对齐**。）
3. **Zenodo 记录页**：确认 Related 的 `isNewVersionOf` 指向 v1.0，作者/机构补全。

## 5. 验收清单（逐项打勾）

- [ ] GitHub 仓库可见、README 渲染正常、有 `v2.0.0` tag 与 Release
- [ ] Zenodo 记录 Published，**Version DOI** 已记录
- [ ] `CITATION.cff` / `README.md` / 论文 三处 DOI 一致
- [ ] Web `/api/stats` 返回 `total: 4117`；`/api/search`、`/api/export.csv` 正常
- [ ] `data/candidates_v2.0.sqlite` 在干净环境可打开并查到 4117 行
- [ ] `docs/REPRODUCE.md` 的 L0 与 Web 命令在干净环境可跑通

## 6. 常见问题

| 现象 | 处理 |
|---|---|
| push 报 `unrelated histories` / 被拒 | 用方案 A 的 `git push -f origin main`，或方案 C 新建仓库 |
| push 要求输密码 | 用 **Personal Access Token** 代替密码，或改 SSH key |
| Zenodo 没自动抓取 | 再点一次 Release；确认 Zenodo 的 GitHub 开关已对该仓库打开；或改手动上传 |
| Web 起来但 `/api/stats` 报 500 | `CAND_DB` 路径不对；确认指向 `data/candidates_v2.0.sqlite` |
| Docker 里查不到数据 | 挂载 `-v <data目录>:/data:ro` 且 `CAND_DB=/data/candidates_v2.0.sqlite` |
| Zenodo 传大文件不便 | `figs/` 放 GitHub，或压缩后作为附加文件上传 |

## 7. 一键脚本（复制粘贴）

```bash
# —— GitHub ——
cd /Users/zhangwei/Documents/Codex/2026-08-30/ni-h/outputs/release_v2/phage-functional-proteins
git remote add origin https://github.com/LPS318/phage-functional-proteins.git 2>/dev/null
git branch -M main
git push -f origin main
git tag -a v2.0.0 -m "Candidate Database v2.0" && git push origin main --tags
# -> 然后到 GitHub 建 Release v2.0.0（Zenodo 自动存档）

# —— Web 本机验收 ——
cd web && pip install -r requirements.txt
CAND_DB=../data/candidates_v2.0.sqlite uvicorn app:app --host 127.0.0.1 --port 8000 &
sleep 3 && curl -s localhost:8000/api/stats
```
